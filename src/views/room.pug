extends template

append styles
    link(rel="stylesheet", href="/style/room.css")

append headScripts
    script(src="socket.io/socket.io.js")

append content
    .container
        .left-board.side-pannel
            ul#players

        .main-board
            h1#roomID #{roomID}
            
            .stateGroup
                p#state CONNECTING
                button#startGame(disabled) Lancer la partie

        .right-board.side-pannel
            ul#codesFeed

append bodyScripts
    script. 
        let roomID = "#{roomID}";
        let name = "#{name}";
    script(src="/scripts/clientRoom.js")